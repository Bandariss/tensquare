server:
  port: 12000
spring:
  application:
    name: tensquare-config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/Bandariss/the_ten_power.git
  rabbitmq:
    host: 192.168.234.128
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh